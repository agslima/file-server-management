[extend]
useDefault = true

[allowlist]
description = "Allow k6 environment variable name TOKEN and dummy JWTs in load-test scripts"
paths = [
  '''file-engine/tests/load/smoke\.js''',
  '''file-engine/tests/load/soak\.js'''
]
regexes = [
  '''__ENV\.(TOKEN|TOKEN_JWT)\b'''
]
